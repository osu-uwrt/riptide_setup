tasks: [
    {
      id: 0,
      name: "Casino_Gate",
      plane: 0,
      search_depth: 1,
      search_accel: 0.8,
      pass_thru_duration: 4.0, # [s]
      end_pos_offset: 1.0, # [m]
      objects: ["Casino_Gate_Black", "Casino_Gate_Red"],
      thresholds: [0.3, 0.3],
      align_thresh: 30, # [pixels]
      bbox_thresh: 30, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 1,
      name: "Path_Marker1",
      plane: 1,
      search_depth: 1,
      search_accel: 0.8,
      objects: ["Path_Marker"],
      thresholds: [0.6],
      align_thresh: 30, # [pixels]
      bbox_thresh: 30, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 2,
      name: "Dice",
      plane: 0,
      search_depth: 2,
      search_accel: 0.8,
      objects: ["Dice1", "Dice2", "Dice5", "Dice6"],
      thresholds: [0.6, 0.6, 0.6, 0.6],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 3,
      name: "Buy_Gold_Chip1",
      plane: 0,
      search_depth: 3,
      search_accel: 0.8,
      objects: ["Buy_Gold_Chip"],
      thresholds: [0.5],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 4,
      name: "Path_Marker2",
      plane: 1,
      search_depth: 2,
      search_accel: 0.8,
      objects: ["Path_Marker"],
      thresholds: [0.6],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 5,
      name: "Slots",
      plane: 0,
      search_depth: 3,
      search_accel: 0.8,
      objects: ["Slots_Fruit", "Slots_Big_Red"],
      thresholds: [0.5, 0.5],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 6,
      name: "Buy_Gold_Chip2",
      plane: 0,
      search_depth: 3,
      search_accel: 0.8,
      objects: ["Buy_Gold_Chip"],
      thresholds: [0.5],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 7,
      name: "Roulette",
      plane: 1,
      search_depth: 2.0,
      search_accel: 0.8,
      objects: ["Roulette"],
      thresholds: [0.5],
      drop_duration_thresh: 4.0,
      align_thresh: 30, # [pixels]
      bbox_thresh: 30, # [pixels]
      detection_duration_thresh: 1.0, # [s]
      detections_req: 8 # [number]
    },
    {
      id: 8,
      name: "Cash_In",
      plane: 0,
      search_depth: 2,
      search_accel: 0.8,
      objects: ["Cash_In"],
      thresholds: [0.5],
      align_thresh: 20, # [pixels]
      bbox_thresh: 20, # [pixels]
      detection_duration_thresh: 1, # [s]
      detections_req: 8 # [number]
    }
  ]
